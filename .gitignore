# Dependencies
node_modules

# Environment variables (contains secrets - never commit!)
.env
.env.local
.env.*.local

# Build output
dist

# Firebase local cache
.firebase

# Local-only files
backup
start.bat
